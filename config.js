// ============================================================
// config.js - 設定ファイル
// ============================================================
// このファイルの値を変更すると、クイズの見た目が変わります！
// 色、サイズ、位置などを自由にカスタマイズしてみましょう。
// ============================================================

// ============================================================
// Step 1: クイズの問題データ
// ============================================================
// 自分だけのクイズを作ってみよう！
//
// kanji: 問題として表示される漢字
// readings: 3つの選択肢（読み方）
// correct: 正解の番号（0 = 1番目、1 = 2番目、2 = 3番目）
//
// 例: { kanji: "山", readings: ["やま", "かわ", "うみ"], correct: 0 }
//     → 「山」の読み方は「やま」が正解（0番目）
// ============================================================
export const quizData = [
    { kanji: "確認", readings: ["かくにん", "かんご", "こくご"], correct: 0 },
    { kanji: "読書", readings: ["どくしょ", "よみしょ", "とくしょ"], correct: 0 },
    { kanji: "勉強", readings: ["べんきょう", "べんこう", "けんきょう"], correct: 0 },
    { kanji: "先生", readings: ["せんせい", "さきなま", "せんじょう"], correct: 0 },
    { kanji: "学校", readings: ["がっこう", "がくこう", "まなこう"], correct: 0 },
    { kanji: "友達", readings: ["ともだち", "ゆうたつ", "ゆうだち"], correct: 0 },
    { kanji: "元気", readings: ["げんき", "もとき", "がんき"], correct: 0 },
    { kanji: "天気", readings: ["てんき", "あまき", "てんけ"], correct: 0 }
];

// ============================================================
// Step 2: 空と地面の色
// ============================================================
// 色は16進数（0xRRGGBB）で指定します
//
// よく使う色:
//   0xFF0000 = 赤      0x00FF00 = 緑      0x0000FF = 青
//   0xFFFF00 = 黄色    0xFF00FF = ピンク  0x00FFFF = 水色
//   0xFFFFFF = 白      0x000000 = 黒      0x808080 = 灰色
//   0x87CEEB = 空色    0x228B22 = 森の緑  0xFFD700 = 金色
//   0xFFA500 = オレンジ 0x800080 = 紫     0xFFC0CB = 薄ピンク
// ============================================================
export const colors = {
    // 背景（空）の色
    skyColor: 0x87CEEB,           // 空色（デフォルト）

    // 地面の色
    groundColor: 0x228B22,        // 森の緑（デフォルト）
};

// ============================================================
// Step 3: 回答ボタンの色
// ============================================================
// 3つの選択肢ボタンの色を設定します
// 配列の順番: [左のボタン, 中央のボタン, 右のボタン]
// ============================================================
export const answerBoxColors = [
    0x66BB6A,   // 緑色（左のボタン）
    0x42A5F5,   // 青色（中央のボタン）
    0xFFA726    // オレンジ色（右のボタン）
];

// ============================================================
// Step 4: 文字の色
// ============================================================
// 色はCSSカラー形式（'#RRGGBB'）で指定します
//
// よく使う色:
//   '#FF0000' = 赤      '#00FF00' = 緑      '#0000FF' = 青
//   '#FFFF00' = 黄色    '#FF00FF' = ピンク  '#00FFFF' = 水色
//   '#FFFFFF' = 白      '#000000' = 黒      '#2C3E50' = 濃い灰色
//   '#E74C3C' = 明るい赤 '#3498DB' = 明るい青
// ============================================================
export const textColors = {
    // 「この漢字の読みは？」の色
    questionTitle: '#2C3E50',     // 濃い灰色

    // 漢字（問題）の色
    kanjiQuestion: '#E74C3C',     // 明るい赤

    // 選択肢の文字色
    answerText: '#2C3E50',        // 濃い灰色

    // 「クイズ終了！」の色
    resultTitle: '#2C3E50',       // 濃い灰色

    // スコア表示の色
    resultScore: '#E74C3C',       // 明るい赤
};

// ============================================================
// Step 4（続き）: 文字のサイズ
// ============================================================
// 数字が大きいほど文字が大きくなります（30〜150くらいが目安）
// ============================================================
export const textSizes = {
    // 「この漢字の読みは？」のサイズ
    questionTitle: 60,

    // 漢字（問題）のサイズ
    kanjiQuestion: 120,

    // 選択肢の文字サイズ
    answerText: 80,

    // 「クイズ終了！」のサイズ
    resultTitle: 80,

    // スコア表示のサイズ
    resultScore: 100,
};

// ============================================================
// Step 5: 回答ボックスの大きさと配置
// ============================================================
export const answerBox = {
    // ボックスのサイズ（幅、高さ、奥行き）
    width: 1.5,
    height: 1.0,
    depth: 0.5,

    // ボックスの高さ（地面からの距離）
    positionY: 1.5,

    // 問題からの距離（大きいほど遠くに配置）
    radius: 4,

    // 選択肢の広がり角度（ラジアン）
    // Math.PI / 3 = 60度、Math.PI / 2 = 90度
    spread: Math.PI / 3,
};

// ============================================================
// Step 5（続き）: 問題の出現位置
// ============================================================
export const questionPosition = {
    // 問題が出現する最小距離
    minDistance: 15,

    // 問題が出現する最大距離
    maxDistance: 25,

    // タイトルの高さ
    titleHeight: 3.5,

    // 漢字の高さ
    kanjiHeight: 2.5,
};

// ============================================================
// Step 6: テキストパネルの背景色（グラデーション）
// ============================================================
// 3つの色でグラデーションを作ります
// ============================================================
export const textPanelColors = {
    // グラデーションの開始色（上部）
    gradientStart: '#FFE5B4',     // ピーチ

    // グラデーションの中間色
    gradientMiddle: '#FFD4A3',    // 薄いオレンジ

    // グラデーションの終了色（下部）
    gradientEnd: '#FFC996',       // 暖かいベージュ

    // 枠線の色
    borderColor: '#E8B88B',       // 茶色
};

// ============================================================
// 照明の設定（上級者向け）
// ============================================================
export const lighting = {
    // メインライト（太陽光のようなもの）
    mainLight: {
        color: 0xffffff,          // 白
        intensity: 1.0,           // 明るさ（0.0〜2.0）
        position: { x: 5, y: 10, z: 5 }
    },

    // バックライト（後ろからの光）
    backLight: {
        color: 0xffffff,
        intensity: 0.8,
        position: { x: -5, y: 5, z: -5 }
    },

    // 環境光（全体を均一に照らす）
    ambientLight: {
        color: 0xffffff,
        intensity: 0.6
    }
};

// ============================================================
// カメラと移動の設定（上級者向け）
// ============================================================
export const camera = {
    // 初期位置
    startPosition: { x: 0, y: 0.6, z: 20 },

    // 視野角（度）
    fov: 75,
};

export const movement = {
    // 移動速度
    moveSpeed: 0.1,

    // 回転速度
    rotationSpeed: 0.03,

    // スティックのデッドゾーン（0.0〜1.0）
    deadZone: 0.15,
};

// ============================================================
// ゲームの設定
// ============================================================
export const gameSettings = {
    // 次の問題までの待ち時間（ミリ秒）
    nextQuestionDelay: 500,

    // 地面のサイズ
    groundSize: 50,
};

// ============================================================
// Step 6: 正解・不正解エフェクトの設定
// ============================================================
// 正解や不正解のときにボタンがどう変化するかを設定します
// ============================================================
export const effectSettings = {
    // エフェクトを有効にする（true = 有効、false = 無効）
    enabled: true,

    // 正解時の設定
    correct: {
        // ボタンの色（緑色でキラキラ）
        color: 0x00FF00,

        // ボタンを光らせる（発光色）
        emissiveColor: 0x00FF00,
        emissiveIntensity: 0.8,

        // ボタンを大きくする（1.0 = 通常、1.5 = 1.5倍）
        scale: 1.3,

        // パーティクル（キラキラ）を表示する
        showParticles: true,
        particleColor: 0xFFD700,  // 金色のキラキラ
        particleCount: 20,        // パーティクルの数
    },

    // 不正解時の設定
    incorrect: {
        // ボタンの色（赤色）
        color: 0xFF0000,

        // ボタンを光らせる（発光色）
        emissiveColor: 0xFF0000,
        emissiveIntensity: 0.3,

        // ボタンを小さくする
        scale: 0.8,

        // 揺れるエフェクト
        shake: true,
    },
};

// ============================================================
// 壁（境界）の設定
// ============================================================
// プレイヤーが移動できる範囲を制限します
// ============================================================
export const wallSettings = {
    // 壁を有効にする（true = 有効、false = 無効）
    enabled: true,

    // 移動可能な範囲（中心からの距離）
    boundaryRadius: 22,

    // 壁を表示する（true = 見える壁、false = 見えない壁）
    visible: true,

    // 壁の高さ
    height: 5,

    // 壁の色
    color: 0x87CEEB,

    // 壁の透明度（0.0 = 完全に透明、1.0 = 完全に不透明）
    opacity: 0.3,

    // 壁の分割数（多いほど滑らかな円になる）
    segments: 32,
};

// ============================================================
// UI テキスト（表示される文字）
// ============================================================
export const uiText = {
    // 問題のタイトル
    questionPrompt: 'この漢字の読みは？',

    // クイズ終了メッセージ
    quizComplete: 'クイズ終了！',

    // スコア表示フォーマット（{score}と{total}は自動で置き換わります）
    scoreFormat: 'スコア: {score}/{total}',

    // バージョン表示
    version: 'v1.0',

    // PC操作説明
    pcControls: 'PC: WASD移動 / QE左右回転 / クリックで回答',

    // VR操作説明
    vrControls: 'Quest 3: VRボタンでVRモード開始',
};
